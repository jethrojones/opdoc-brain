<!-- AI-MEMORY-VAULT:START -->
## AI Memory Vault

My external memory is the Obsidian vault at `[VAULT PATH]`. Treat it as the single source of truth shared across AI tools. Keep project source code in its own repository; keep durable context, decisions, playbooks, and work logs in the vault.

At the start of every session:

1. Read `[VAULT PATH]/VAULT-INDEX.md`.
2. Check yesterday’s daily note in `[VAULT PATH]/01 - Daily Notes/` and backfill it only when you have relevant missing context.
3. Read `[VAULT PATH]/Active Priorities.md`.
4. Read `[VAULT PATH]/00 - Inbox/Agent Tasks.md`.

Rules that cannot lapse:

- The vault is my memory. Persist meaningful outcomes in the appropriate note and keep folder indexes in sync.
- Keep the vault index short and curated; link to detail instead of expanding it into an encyclopedia.
- Name source files for important claims and decisions.
- Never silently delete, publish, send, or expose vault material.
<!-- AI-MEMORY-VAULT:END -->
